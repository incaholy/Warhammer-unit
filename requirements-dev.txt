# Dev/test dependencies — installed by `make install-dev`.
# Pulls in everything from requirements.txt, plus dev-only tools below.
-r requirements.txt

# (pytest / pytest-cov currently live in requirements.txt; move them here if you
#  want to keep them out of production installs.)

# web scraping (catalog scraper) — see SPEC "Scraping the catalog (Wahapedia)"
beautifulsoup4==4.15.0
lxml==6.1.1
ruff==0.14.4
